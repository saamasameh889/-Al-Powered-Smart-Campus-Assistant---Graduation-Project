# Zewail Campus Digital Assistant — Dependencies
# Install with: pip install -r requirements.txt
#
# Covers all five products:
#   Product A — RAG Academic Advisor (phases 1-7)
#   Product B — Learning Analytics & XAI Dashboard (learning_analytics_xai/)
#   Product C — GPA Trajectory Forecasting (LSTM)
#   Product D — Student Archetype Clustering (GMM)
#   Product E — GitHub Career Advisor

# ── Web scraping (Product A — offline phase only, not needed at runtime) ──────
requests==2.32.3
beautifulsoup4==4.12.3

# ── PDF extraction (Product A) ───────────────────────────────────────────────
PyMuPDF==1.27.1

# ── Embeddings + LLM (Product A) ─────────────────────────────────────────────
openai==1.109.1
tiktoken==0.8.0

# ── Vector store (Product A) ─────────────────────────────────────────────────
chromadb==0.5.23

# ── LangChain (Product A RAG helpers) ────────────────────────────────────────
langchain>=0.3.15,<0.4
langchain-core>=0.3.52,<0.4
langchain-openai>=0.3.2,<0.4
langchain-chroma==0.2.3
langchain-text-splitters>=0.3.5,<0.4
langchain-community>=0.3.15,<0.4

# ── Machine Learning (Product B) ─────────────────────────────────────────────
xgboost==3.2.0
lightgbm==4.6.0
scikit-learn==1.9.0

# ── Explainability / XAI (Product B) ─────────────────────────────────────────
shap==0.52.0

# ── Data processing ───────────────────────────────────────────────────────────
pandas>=2.2,<3
numpy>=2.1,<3

# ── Visualisation ─────────────────────────────────────────────────────────────
plotly==6.8.0
matplotlib==3.11.0

# ── Streamlit UI ──────────────────────────────────────────────────────────────
streamlit==1.41.1

# ── Google Classroom integration (Product B — XAI import) ────────────────────
google-api-python-client>=2.100.0
google-auth-oauthlib>=1.1.0
google-auth-httplib2>=0.2.0

# ── Utilities ────────────────────────────────────────────────────────────────
python-dotenv==1.0.1
httpx==0.28.1

# ── Deep learning — Product C (GPA Forecasting LSTM) ─────────────────────────
# CPU-only wheel — Streamlit Cloud / Linux compatible
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.6.0+cpu

# ── Dimensionality reduction — Product D (Student Clustering) ────────────────
umap-learn==0.5.7
pynndescent==0.5.13

# ── Anthropic SDK — Product E (Career Advisor model comparison) ──────────────
anthropic>=0.40.0

# ── Graph analysis — Academic advisor prerequisite engine ────────────────────
networkx>=3.3
